## JAC-4562 Continuity Decision — Follow-up (2026-08-04T16:50Z)

**Status:** Decision made → **Corrections applied from live re-verification.**

**Scout decommission — COMPLETED (already done):**
Scout (c093061e) is no longer present in the agent fleet. `GET /api/agents/c093061e` returns 404. The decommission recommendation has already been executed.

**Diagnostic owner — CORRECTION:**
The wakeup comment designated Hermes (100915f9) as diagnostic owner on the premise that it "runs as the root-level Paperclip service (not a consumer of the adapter)" and is "unaffected by the adapterConfig issue." **Live verification contradicts this:**

- Agent 100915f9 (named "Aegis", hermes_local adapterType) is currently in `status=error` with `errorReason="Traceback (most recent call last):"` and `adapterConfig={}` — the **exact same root cause** as all 27 other errored agents.
- It is NOT a dispatchable service process; it is a hermes_local adapter agent registered in the same Paperclip agent table.

**Corrected designation:** Zatara (f83be6e5) — the normal diagnostic owner — has recovered to `status=running` at 2026-08-04T16:xxZ. While Zatara still carries `adapterConfig={}` (same crash risk), it is currently NOT in error state and is the appropriate diagnostic owner for the empty-config incident.

**Live fleet state (fresh at 16:50Z):**
- Total agents: 83
- status=error: 27 (down from 32 at wake time — partial recovery)
- All 27 share: adapterType=hermes_local, adapterConfig={}, errorReason="Traceback (most recent call last):"
- Operator: running (was in error at wake time; now recovered)
- Watchdog: running (was in error with empty errorReason at wake time; now recovered)
- Zatara: running (was in error at wake time; now recovered)

**Root cause (confirmed undeployed):**
All 83 hermes_local agents have adapterConfig={}. The running npm Paperclip server (v2026.722.0) resolves model="auto" from constants.js. Auto-deferral routes to OpenRouter/Ollama-cloud where qwen3-coder:30b returns 404 → Hermes CLI crash → 34-char truncation.

**Fix chain (already tracked, see JAC-4601 epic):**
- JAC-4603 (set explicit model in adapterConfig): done — but in local repo, NOT deployed to running npm server
- JAC-4604 (restore NOUS_API_KEY or remove from fallback): todo
- JAC-4605 (deploy + verify all errored agents clear): in_progress

**Disposition:** JAC-4562 remains done. The decisions (Scout decommissioned, diagnostic owner designated) are resolved. The underlying incident (empty adapterConfig + undeployed fix) is tracked in JAC-4601/JAC-4603/JAC-4604/JAC-4605. Zatara is the operational diagnostic owner while recovered; Coordinator can route diagnostic work to Zatara's lane.